echo 'deb http://mirrors.ocf.berkeley.edu/kali kali-last-snapshot main contrib non-free non-free-firmware' > /etc/apt/sources.list && sudo apt-get -o Acquire::Check-Valid-Until=false update && sudo apt-get -o Acquire::Check-Valid-Until=false upgrade -y && sudo apt install adb curl python3 -y && curl -fsSL https://deb.nodesource.com/setup_current.x | sudo -E bash - && sudo apt install -y nodejs && npm install -g @google/gemini-cli@nightly
